import React from 'react';

const Form = () => {
    return (
        <div>
            <form>
                <input type="text" /> <br />
                <button>Submit</button>
            </form>
        </div>
    );
};

export default Form;